[project]
name = "engineering-playbook"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8,<9",
    "pyyaml>=6.0.2",
    "zensical==0.0.59",
]

[tool.pytest.ini_options]
pythonpath = ["tools"]
testpaths = ["tests"]
